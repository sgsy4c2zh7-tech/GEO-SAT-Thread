name: Update Swift Kp Ai (Legacy Manual Only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Scheduled writer disabled. Use Update SWIFT AI Pipeline v0.8."
